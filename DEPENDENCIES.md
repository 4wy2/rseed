# Dependencies

المصدر الحقيقي لتعريف dependencies هو `package.json`.

لتثبيتها:

```bash
npm install
```

لم يتم تضمين `node_modules/` لأنه artifact مولّد وليس Source Code، وهو مستبعد أيضًا من Git وفق الممارسة القياسية.
